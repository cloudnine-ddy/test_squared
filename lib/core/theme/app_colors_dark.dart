import 'package:flutter/material.dart';

/// Dark theme color palette
class AppColorsDark {
  // Dark warm theme
  static const background = Color(0xFF1A1A1A);        // Dark charcoal
  static const surface = Color(0xFF252525);           // Slightly lighter
  static const sidebar = Color(0xFF2D2D2D);           // Even lighter
  static const primary = Color(0xFF5B8DB8);           // Lighter navy blue
  static const accent = Color(0xFFC4A570);            // Warm gold
  static const textPrimary = Color(0xFFE8E8E8);       // Off-white
  static const textSecondary = Color(0xFFB0B0B0);     // Medium gray
  static const border = Color(0xFF3A3A3A);            // Subtle borders
  static const shadow = Color(0xFF000000);            // Black shadow
}
